# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Divent' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Divent
	pod 'Firebase'
        pod 'Firebase/Database'
        pod 'Firebase/Auth'
        pod 'Firebase/Core'
	pod 'FacebookShare'
	pod 'GoogleAnalytics'

  target 'DiventTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'DiventUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
